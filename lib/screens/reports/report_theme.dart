// lib/screens/reports/report_theme.dart
//
// Shared color tokens for the Reports module — mirrors the tokens
// used in the main dashboard so report screens feel native to the app.

import 'package:flutter/material.dart';

class RTheme {
  RTheme._();

  static const bg          = Color(0xFF050A14);
  static const navy        = Color(0xFF030710);
  static const surface     = Color(0xFF0A1428);
  static const surfaceHigh = Color(0xFF0F1C35);
  static const border      = Color(0xFF1A2E50);

  static const blue      = Color(0xFF1E5FC8);
  static const blueLight = Color(0xFF3A7AE8);
  static const blueDark  = Color(0xFF0D3A80);

  static const green = Color(0xFF00D68F);
  static const red   = Color(0xFFE8374A);

  static const textPrimary = Color(0xFFF0F6FF);
  static const textSub     = Color(0xFFA0B8D0);
  static const textMuted   = Color(0xFF4A6080);
}